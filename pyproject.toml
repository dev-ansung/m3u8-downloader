[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "stream-fetch"
version = "0.1.0"
description = "Download HLS streams and direct video URLs to MP4"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "m3u8>=3.5",
    "pycryptodome>=3.19",
    "tqdm>=4.66",
]

[project.scripts]
stream-fetch = "stream_fetch.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/stream_fetch"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-mock>=3.15.1",
    "requests-mock>=1.12.1",
]
